package FullStackAutomation.androidTests;

import FullStackAutomation.androidSetup.Android_BaseTest_MobileBrowser;

public class MobileBrowserTestAndroid extends Android_BaseTest_MobileBrowser {

    /*@Test
    public void launchMobileBrowserOnApp() throws InterruptedException {
        Thread.sleep(3000);
        driver.get("https://www.google.com");
        driver.findElement(By.name("q")).sendKeys("appium");
        driver.findElement(By.name("q")).sendKeys(Keys.ENTER);
    }*/
    // boave code will not work as one the emulator chrome is not getting identified
}
